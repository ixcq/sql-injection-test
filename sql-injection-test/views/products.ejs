<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ürün Kartları</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <div class="row">
            <% for(let i = 0; i < products.length; i++) { %>
            <div class="col-md-6 mb-4">
                <!-- Ürün kartını link yap -->
                <a href="/product/<%= products[i].id %>" class="text-decoration-none">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body d-flex">
                            <div class="flex-grow-1">
                                <h5 class="card-title text-dark"><%= products[i].name %></h5>
                                <p class="card-text text-muted"><%= products[i].desc %></p>
                                <p class="text-primary fw-bold"><%= products[i].price %> TL</p>
                                <small class="text-muted">Detaylar için tıklayın →</small>
                            </div>
                            <div class="ms-3">
                                <img src="<%= products[i].img %>" class="rounded" alt="<%= products[i].name %>" width="80" height="80">
                            </div>
                        </div>
                    </div>
                </a>
            </div>
            <% } %>
        </div>
    </div>
</body>
</html>